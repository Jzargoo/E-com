spring:
  application:
    name: media-service
  profiles:
    active: devconfig
  config:
    import: configserver:http://localhost:8888
  grpc:
    server:
      port: 8081
  cloud:
    aws:
      region:
        static: kz-central-1
application:
  native-storage-options:
    tempDirectory: media-service-temp
  portion: 65536  # 64 * 1024